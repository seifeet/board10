<% page = valid_page_or_one params[:page] %>
<% @messages = current_user.recieved.paginate(:page => page, :per_page => per_page ) if @messages.nil? %>
<%= render 'shared/containers/page_container', :page_container => @messages %>