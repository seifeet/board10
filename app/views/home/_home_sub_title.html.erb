<%= render 'home/home_sub_title_impl', :board => @board, :school => @school,
    :other_title => @postings_title,
    # elements for autorefresh
    :form_obj => @autorefresh, :from => @from_posting, :results => @postings %>
