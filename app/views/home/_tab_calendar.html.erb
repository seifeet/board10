<%
if @board
  element = @board
elsif @school
  element = @school
else
  element = @posting
end
%>

<%= render 'shared/calendars/calendar_panel', :controller_path => home_path,
            :element => element, :given_date => @date, :cal_elements => @events %>
