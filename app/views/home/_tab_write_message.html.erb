<% if !params[:act].nil? && params[:act] == 'write' && !current_user.nil? %>
<div class="close_button inline_right">
  <%= link_to "Close", home_path + '?act=cancel', :title => "Close Messages" %>
</div>
<div id="action_tab">
<%= render 'board_posting_form' %>
</div>
<% end %>