<% return nil if current_user.nil? %>

<div id="user_title_nav">
	<div class="nav_link">
	<% if current_user.recieved.unread.count > 0 %>
	<div class="background_write"><font color="red">Incoming...</font></div>
	<br />
	<%= link_to "Messages (#{current_user.recieved.unread.count} new)", home_path + '?act=messages' %>
	<% else %>
	<div class="background_write">Messages</div>
	<br />
	<%= link_to "Messages", home_path + '?act=messages' %>
	<% end %>    
    </div>
    <br class="clearBoth" />
	<div class="background_write">Schools</div>
	<br class="clearBoth" />
	<div id="user_schools"><%= render 'home/user_schools', :user => current_user %></div>
	<br class="clearBoth" />
	<div class="background_write">Boards</div>
	<br />
	<div class="nav_link"><%= link_to "All My Boards", home_path + '?act=boards' %></div>
	<br class="clearBoth" />
	<br />
	<div id="user_boards"><%= render 'home/user_boards', :user => current_user %></div>
	<br class="clearBoth" />
	<br />
	<div class="background_write">What's new?</div>
	<br />
	<div class="http_link"><%= link_to "Users", users_path %></div>
	<div class="http_link"><%= link_to "Schools", schools_path %></div>
	<div class="http_link"><%= link_to "Boards", boards_path %></div>
	<div class="http_link"><%= link_to "Posts", postings_path %></div>
</div>
