<% if params[:act] == 'invite' && !params[:search].nil? %>
    $("#action_area").html("<%= escape_javascript(render 'boards/tab_invite_search') %>");
	document.title = "<%= escape_javascript("Search #{params[:search].to_s.titleize} in Users") %>";
        
<% elsif params[:act] == 'invite' %>
    $("#action_tab").html("<%= escape_javascript(render 'boards/tab_invite_results') %>");
    document.title = "Invite people";
<% end %>

$(".loading").html("");
