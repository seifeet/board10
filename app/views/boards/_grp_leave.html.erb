<%= form_for current_user.members.find_by_board_id(@board), 
             :html => { :method => :delete } do |f| %>
  <div class="actions"><%= f.submit "Leave This Board", :class => "button_reject" %></div>
<% end %>
