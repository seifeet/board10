<%
if @board
  element = @board
  curr_board_path = board_path(@board)
elsif @school
  element = @school
  curr_board_path = board_path(@board)
else
  element = @posting
  curr_board_path = board_path(@posting.board_id)
end
%>

<%= render 'shared/calendars/calendar_panel', :controller_path => curr_board_path,
           :element => element, :given_date => @date, :cal_elements => @events %>