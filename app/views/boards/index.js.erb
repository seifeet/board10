$("#board_search_container").html("<%= escape_javascript(render 'shared/containers/search_container', :object_type => 'board') %>" );
document.title = "<%= escape_javascript("#{params[:search].to_s.titleize} boards by #{(params[:sort] || 'name').titleize} - Page #{params[:page] || 1}") %>";
