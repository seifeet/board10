$("#school_search_container").html("<%= escape_javascript(render 'shared/containers/search_container', :object_type => 'school') %>" );
document.title = "<%= escape_javascript("#{params[:search].to_s.titleize} schools by #{(params[:sort] || 'name').titleize} - Page #{params[:page] || 1}") %>";
