<%= form_for(@posting) do |f| %>
  <% if @posting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@posting.errors.count, "error") %> prohibited this posting from being saved:</h2>
      <ul>
      <% @posting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= hidden_field_tag 'group_id', params[:group_id] %>
  <div class="field">
    <%= f.label :subject %><br />
    <%= f.text_field :subject, :size => 40 %>
  </div>
  <div class="field">
    <%= f.label :content %>
    <%= f.radio_button 'visibility', '2' %><%= f.label :visibility ,'Public' %>
    <%= f.radio_button 'visibility', '1' %><%= f.label :visibility ,'Protected' %>
    <%= f.radio_button 'visibility', '0' %><%= f.label :visibility ,'Private' %><br />
    <%= f.text_area :content, :rows => 7, :cols => 40 %>
  </div>
  <div class="actions">
    <%= f.submit 'Add Post' %>
  </div>
<% end %>
