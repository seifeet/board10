<% flash.delete(:recaptcha_error) if flash[:recaptcha_error] && !flash[:recaptcha_error].empty? %>
<% if flash.any? %>
<% flash.each do |key, value| %>
<div class="flash_<%= key %> clearfix">
<div class="inline"><%= value %></div>
</div>
<% end %>
<% end %>
