<% if @group.active? %>
<%= form_for @group.members.build(:user_id => 50) do |f| %>
  <div><%= f.hidden_field :user_id %></div>
  <div class="actions"><%= f.submit "Invite To This Group" %></div>
<% end %>
<% end %>