<% if @group.active? %>
<%= form_for @group.members.find_by_user_id(@user.id), :html => { :method => :delete } do |f| %>
  <div class="actions"><%= f.submit "Ban #{@user.full_name}" %></div>
<% end %>
<% end %>