<% return nil if params[:board].nil? || @board.nil? %>
<% @board = Board.find_board(params[:board]) if @board.nil? %>
<% if @board.active? && current_user.member?( @board.id ) %>
<% user = User.find_user(params[:id]) %>
<% @reply = false %>
<%= render 'boards/posting_form' %>
<% end %> 