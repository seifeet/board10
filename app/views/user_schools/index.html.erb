<h1>Listing user_schools</h1>

<table>
  <tr>
    <th>User</th>
    <th>School</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_schools.each do |user_school| %>
  <tr>
    <td><%= user_school.user_id %></td>
    <td><%= user_school.school_id %></td>
    <td><%= link_to 'Show', user_school %></td>
    <td><%= link_to 'Edit', edit_user_school_path(user_school) %></td>
    <td><%= link_to 'Destroy', user_school, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User school', new_user_school_path %>
